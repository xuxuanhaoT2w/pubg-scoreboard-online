# DESIGN.md — 开黑计分板（PUBG）

## 产品定位
固定车队局后零和记账工具：击杀分 + 吃鸡分，跨局累计。**桌面网页版**：一个晚上连打多局，桌前逐局录入、总分逐局自动叠加，大屏表格一览无余。沿用暗色军事电竞视觉（仍可在浏览器直接打开、PWA 离线），但布局以宽屏桌面操作为主。

## 气质与意象
- 意象锚点：**夜间战术行动简报板**。跳伞前的黑伞布、空投箱的压纹铁皮、夜视仪 HUD 的琥珀色读数。
- 整体像一块架在弹药箱上的战术终端：哑光炭黑底、斜切边角的硬边卡片、铆钉感分隔线、细网格纹理。
- 情绪关键词：肃杀、利落、街头战术；像赛前战术板，不像游戏卡通 UI。

## 配色方案
| Token | 色值 | 意象来源 |
| --- | --- | --- |
| `bg` | `#0d0f0c` | 夜间作战服底色（偏暖的近黑） |
| `panel` | `#161812` | 空投箱哑光铁皮 |
| `panel-2` | `#1e2118` | 抬升一层的卡片 |
| `line` | `#2c2f24` | 战术装备接缝 |
| `primary` | `#f5a623` | PUBG 信号黄（空投烟雾 / 品牌橙黄） |
| `primary-press` | `#d98f12` | 按钮按下态 |
| `gain` | `#ffc53d` | 得分：琥珀金高亮 |
| `loss` | `#e5484d` | 失分：暗红警示 |
| `text` | `#ecead9` | 做旧米白（战术地图纸色） |
| `muted` | `#8a8d7a` | 军绿灰次要文字 |

规则：黄色只给「主行动 / 得分 / 选中 / 夺冠」；失分固定暗红；绝不用蓝紫渐变。

## 字体排版
- 数字与英文标题：**Oswald**（窄长 condensed，HUD 读数感），分数数字大号加粗。
- 中文正文：**Noto Sans SC**。
- 通过 `fonts.googleapis.cn` 引入（CN 域），`font-display: swap`。
- 分数变化用 tabular 数字 + 正负号（`+3` / `-1`），字号显著大于周边文字。

## 布局与组件（桌面优先）
- **顶部固定导航栏**：左侧 Logo（准星图标 + 开黑计分板）+ 主导航（排行榜 / 记一局 / 历史 / 设置），「记一局」为信号黄实心主按钮；内容区宽容器 `max-w-5xl/6xl` 居中，桌面分栏（主区 + 侧栏），窄屏自动堆叠。
- 「记一局」为**单页表格录入**：表格每行一名队员（默认全员参战，可取消勾选剔除），列为：参战勾选 / 队员 / **击杀数**（− 按钮 + 可键盘直接输入的数字框 + + 按钮）/ 吃鸡勾选；右侧固定面板实时显示本局每人 `+x/−x`、零和校验与**当前累计总分**，连录时可见每局叠加效果。
- 卡片 12px 圆角，关键卡片做**斜切角**（`clip-path` 切 1~2 个角）呼应战术风。
- 击杀数字字段：大号 Oswald、信号黄，支持键盘直接键入与 ±按钮微调（按钮仍可长按连发，桌面也适用）。
- 选择态：参战/吃鸡勾选 → 黄色描边 + 角标对勾；吃鸡选中叠加 Trophy 图标 + 「吃鸡」标记。
- 排行榜/历史：桌面用宽表格或多列网格，一行多人信息，得分右对齐大号数字。
- 结算预览面板：合计恒为 0 时显示「零和校验 ✓」；保存后表单复位（保留参战勾选，便于同车队连录下一局），不强制跳转。

## 动效与交互
- 性格：机械、干脆，像枪械部件咔哒入位。过渡 120–200ms，`cubic-bezier(0.2, 0.8, 0.2, 1)`。
- 数字调整：数值轻微 `scale` 脉冲；导航切换淡入即可。
- 保存成功：顶部 toast「战绩已入库」，可留在记一局页继续录下一局。
- 删除对局/队员：二次确认弹窗（红色危险按钮），确认后移除。

## 设计禁忌
- 不要科技蓝、蓝紫渐变、霓虹发光赛博风。
- 不要圆角超过 14px 的软萌卡片、不要彩色渐变背景。
- 不要手机竖屏底部 Tab/超大触控按钮那套移动布局；以桌面宽屏表格为主。
- 不要用 emoji 充当功能图标（统一 lucide 线性图标，鸡晚餐用 Trophy）。
